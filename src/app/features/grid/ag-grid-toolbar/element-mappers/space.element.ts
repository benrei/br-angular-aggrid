import { ColDef } from 'ag-grid-community';
import { ElementBase } from './element.base';

export class SpaceElement extends ElementBase {
  constructor(columnDef: ColDef) {
    super(columnDef);
  }
}
